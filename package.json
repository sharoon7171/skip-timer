{
  "name": "countdown-bypass",
  "version": "1.0.0",
  "description": "Automatically bypass countdown timers and waiting periods to provide instant access to content and download links",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "watch": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/node": "^24.5.2",
    "terser": "^5.24.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
